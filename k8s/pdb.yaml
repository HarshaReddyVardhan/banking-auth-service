apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: banking-auth-service-pdb
  namespace: banking
spec:
  # Ensure at least 2 pods are always available
  minAvailable: 2
  selector:
    matchLabels:
      app: banking-auth-service
